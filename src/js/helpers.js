const ENTER_KEY = 13, // El key code del enter es 13. Lo guardamos en la constante preferiblemente. Más semántico se vuelve el código
    c = console.log,
    d = document,
    j = JSON,
    ls = localStorage // Vamos a hacer uso del API storage para guardar las tareas pendientes del usuario en su navegador.

export {
    ENTER_KEY,
    c,
    d,
    j,
    ls
}